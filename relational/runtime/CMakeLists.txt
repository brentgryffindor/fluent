CMAKE_MINIMUM_REQUIRED(VERSION 3.6)

MACRO(CREATE_RUNTIME_TEST NAME)
    CREATE_NAMED_TEST(${NAME} ${NAME})
    TARGET_LINK_LIBRARIES(${NAME} zmq flzmq)
    ADD_DEPENDENCIES(${NAME} flzmq zeromq zeromqcpp)
ENDMACRO(CREATE_RUNTIME_TEST)

CREATE_RUNTIME_TEST(builder_test)